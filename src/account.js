import {escapeHTML,formatDate,formatNumber} from './utils.js';
export const ACCOUNT_CONFIG={maxAvatarBytes:5*1024*1024,allowedMimeTypes:['image/jpeg','image/png','image/webp'],presenceMinutes:5};
export async function api(path,options={}){const raw=options.body instanceof ArrayBuffer;const response=await fetch(path,{credentials:'same-origin',...options,headers:raw?options.headers:{'content-type':'application/json',...(options.headers||{})}});let data={};try{data=await response.json();}catch{}if(!response.ok)throw new Error(data.error||'Não foi possível concluir esta operação.');return data;}
export const initials=name=>String(name||'Aluno').trim().split(/\s+/).slice(0,2).map(x=>x[0]?.toUpperCase()).join('');
export function avatar(user,size=''){return user?.hasAvatar?`<img class="avatar ${size}" src="/api/avatar?v=${user.avatarVersion||0}" alt="Foto de perfil">`:`<span class="avatar ${size}">${escapeHTML(initials(user?.name))}</span>`;}
export function authScreen(mode='login',token=''){const register=mode==='register',forgot=mode==='forgot',reset=mode==='reset';return `<div class="auth-shell"><section class="auth-brand"><img src="assets/fixou-logo.png" alt="FIXOU"><span>ESTUDO QUE FICA</span><h1>Seu aprendizado continua de onde você parou.</h1><p>Treinos, revisões, domínio e conquistas salvos com segurança na sua conta.</p></section><section class="auth-card"><div class="eyebrow">${register?'CRIAR CONTA':forgot||reset?'RECUPERAR ACESSO':'BEM-VINDO DE VOLTA'}</div><h2>${register?'Comece sua jornada':forgot?'Redefina sua senha':reset?'Escolha uma nova senha':'Entre no FIXOU'}</h2><form id="${reset?'reset-form':forgot?'forgot-form':register?'register-form':'login-form'}" class="form-stack">${register?'<label>Nome<input name="name" required minlength="2" maxlength="80" autocomplete="name"></label>':''}${!reset?'<label>E-mail<input name="email" type="email" required maxlength="160" autocomplete="email"></label>':''}${!forgot?'<label>'+(reset?'Nova senha':'Senha')+'<input name="password" type="password" required minlength="8" maxlength="128"></label>':''}${register?'<label>Confirmar senha<input name="confirmPassword" type="password" required minlength="8" maxlength="128"></label>':''}${reset?`<input type="hidden" name="token" value="${escapeHTML(token)}">`:''}<button class="primary" type="submit">${register?'Criar minha conta':forgot?'Enviar instruções':reset?'Salvar nova senha':'Entrar'} →</button></form><div class="auth-links">${register?'<a href="#login">Já tenho uma conta</a>':forgot||reset?'<a href="#login">Voltar ao login</a>':'<a href="#register">Criar conta</a><a href="#forgot">Esqueci minha senha</a>'}</div><p class="note">Ao continuar, seus dados de estudo ficam vinculados à sua conta FIXOU.</p></section></div>`;}
const metric=(label,value,note='')=>`<article class="admin-metric"><span>${label}</span><strong>${formatNumber(value||0)}</strong><small>${note}</small></article>`;
function bars(series,key,color='var(--gold)'){const max=Math.max(1,...series.map(x=>x[key]||0));return `<div class="mini-chart">${series.map(x=>`<div class="chart-column"><span style="height:${Math.max(3,Math.round((x[key]||0)/max*100))}%;background:${color}" title="${x.date}: ${x[key]||0}"></span><small>${x.date.slice(5)}</small></div>`).join('')}</div>`;}
export function adminScreen({adminData,adminPeriod=30}){if(!adminData)return '<div class="loading-screen"><span class="spinner"></span><h2>Carregando dados reais…</h2></div>';const d=adminData,m=d.metrics||{},users=d.users||[],reports=d.reports||[],suggestions=d.suggestions||[];return `<div class="admin-head"><div><div class="eyebrow">ADMIN · PAINEL DO DONO</div><h1>Visão geral do FIXOU</h1><p>Uso real, aprendizagem e sinais que pedem atenção.</p></div><div class="segmented">${[7,30,90].map(n=>`<button data-action="admin-period" data-id="${n}" class="${adminPeriod===n?'selected':''}">${n} dias</button>`).join('')}</div></div><div class="admin-metrics">${metric('Online agora',m.online,'últimos 5 minutos')}${metric('Ativos hoje',m.dau,'DAU')}${metric('Ativos em 7 dias',m.wau,'WAU')}${metric('Ativos em 30 dias',m.mau,'MAU')}${metric('Novos hoje',m.newToday)}${metric('Respostas hoje',m.answersToday)}${metric('Sessões iniciadas',m.sessionsToday)}${metric('Sessões concluídas',m.completedToday)}${metric('Flashcards hoje',m.flashcardsToday)}</div><div class="admin-grid"><section class="panel"><div class="section-title"><h2>Usuários ativos</h2><span>Dados reais</span></div>${bars(d.series||[],'active','var(--sky)')}</section><section class="panel"><div class="section-title"><h2>Questões corretas</h2><span>${d.counts?.correct||0} acertos · ${d.counts?.incorrect||0} erros</span></div>${bars(d.series||[],'correct')}</section></div><div class="admin-grid"><section class="panel"><h2>Funil de ativação</h2><div class="funnel">${(d.funnel||[]).map(x=>`<div><span>${escapeHTML(x.label)}</span><strong>${x.value}</strong><small>${x.conversion}% da etapa anterior</small></div>`).join('')}</div></section><section class="panel"><h2>Funil de estudos</h2><div class="funnel">${(d.studyFunnel||[]).map(x=>`<div><span>${escapeHTML(x.label)}</span><strong>${x.value}</strong></div>`).join('')}</div></section></div><section class="panel admin-table"><div class="section-title"><h2>Usuários</h2><span>${users.length} contas</span></div><div class="table-scroll"><table><thead><tr><th>Aluno</th><th>Plano</th><th>Role</th><th>Cadastro</th><th>Última atividade</th></tr></thead><tbody>${users.map(u=>`<tr><td><span class="table-person"><span class="avatar tiny">${initials(u.name)}</span><span><strong>${escapeHTML(u.name)}</strong><small>${escapeHTML(u.email)}</small></span></span></td><td>${u.plan}</td><td>${u.role}</td><td>${formatDate(u.created_at)}</td><td>${u.last_activity_at?formatDate(u.last_activity_at):'—'}</td></tr>`).join('')||'<tr><td colspan="5">Ainda não há usuários.</td></tr>'}</tbody></table></div></section><div class="admin-grid"><section class="panel"><div class="section-title"><h2>Questões reportadas</h2><span>${reports.length}</span></div>${reports.slice(0,10).map(r=>`<article class="admin-inbox"><strong>${escapeHTML(r.question_id)}</strong><span>${escapeHTML(r.reason)} · ${r.status}</span><p>${escapeHTML(r.comment||'Sem comentário')}</p></article>`).join('')||'<p class="soft-copy">Nenhuma questão reportada.</p>'}</section><section class="panel"><div class="section-title"><h2>Sugestões</h2><span>${suggestions.length}</span></div>${suggestions.slice(0,10).map(s=>`<article class="admin-inbox"><strong>${escapeHTML(s.category)}</strong><span>${s.status}</span><p>${escapeHTML(s.message)}</p></article>`).join('')||'<p class="soft-copy">Nenhuma sugestão recebida.</p>'}</section></div>`;}
